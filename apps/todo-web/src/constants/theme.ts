import { type Theme } from '@nextui-org/react';

export const LIGHT_THEME: Theme = {
  type: 'light',
  theme: {
    fonts: {},
  },
};

export const DARK_THEME: Theme = {
  type: 'dark',
  theme: {
    fonts: {},
  },
};
