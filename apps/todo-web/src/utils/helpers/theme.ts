import { createTheme } from '@nextui-org/react';

import { DARK_THEME, LIGHT_THEME } from '../../constants';

export const lightTheme = createTheme(LIGHT_THEME);

export const darkTheme = createTheme(DARK_THEME);
